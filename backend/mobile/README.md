# Mobile App Scaffold

This folder prepares the repository for a future mobile app.

Suggested next steps:
- Pick a client framework: Flutter or React Native.
- Use `/api/mobile/*` for mobile-specific backend APIs.
- Keep mobile UI and web templates separate.
- Current temporary backend base URL:
  `https://tb80wxsm-5000.inc1.devtunnels.ms/api/mobile`

Folder overview:
- `mobile/app/`: mobile client source code
- `mobile/assets/`: icons, images, fonts
- `mobile/config/`: environment and app config
- `mobile/docs/`: mobile architecture notes
- `mobile/tests/`: mobile test placeholders
